---
description: Show details of a specific task
---

Show the full details of a task by its ID.

Run:
```bash
${CLAUDE_PLUGIN_ROOT}/scripts/task.sh show $ARGUMENTS
```

`$ARGUMENTS` should be a task ID (number). Show the output to the user.
