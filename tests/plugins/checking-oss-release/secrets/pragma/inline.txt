# Inline pragma silences a finding on the same line.
# `{{X}}` is stripped by tests/run.sh; see detect/secrets.txt for context.
AK{{X}}IAIOSFODNN7EXAMPLE  # oss-checker:allow
gh{{X}}p_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa  # oss-checker:allow: known fixture
